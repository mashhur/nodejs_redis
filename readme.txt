1. type a command to install redis '$ npm install redis'
2. package.json should be located in current package
3. type '$ npm install' command to install node modules for current project
4. redis should run
